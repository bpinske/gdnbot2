/**
 * Scrape the user's SA profile for their SA ID
 *
 * @param {object} tag - The output from a call to logger.getLogTag()
 * @param {string} username - The member's SA Username
 * @return {object} - { id }
 */
const getSAID = ({ tag, username }) => {

};

module.exports = getSAID;
